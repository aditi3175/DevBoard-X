import { query, mutation } from "./_generated/server";
import { v } from "convex/values";

export const getAnalytics = query({
  handler: async (ctx) => {
    // Placeholder for fetching analytics (e.g. project completion rates)
    return {};
  },
});
